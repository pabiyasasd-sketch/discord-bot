# DiscordBot [old code]
# import discord
# from botLogic import flip_coin, gen_pass, gen_emoji
#
# # Variable intents stores the bot's privileges
# intents = discord.Intents.default()
# # Activate the message-reading privilege
# intents.message_content = True
# # Make a bot in the variable client and transfer it privileges
# client = discord.Client(intents=intents)
#
# @client.event
# async def on_ready():
#     print(f'Kita telah masuk sebagai {client.user}')
#
# @client.event
# async def on_message(message):
#     if message.author == client.user:
#         return
#     if message.content.startswith('$halo'):
#         await message.channel.send("Hi!")
#     elif message.content.startswith('$bye'):
#         await message.channel.send("\U0001f642")
#     elif message.content.startswith('$pwgen'):
#         password = gen_pass(10)  # Generate a 10-character password
#         await message.channel.send(f"Generated password: {password}")
#     elif message.content.startswith('$cflip'):
#         result = flip_coin()  # Flip a coin
#         await message.channel.send(f"The coin landed on: {result}")
#     elif message.content.startswith('$emoji'):
#         emoji = gen_emoji()  # Generate a random emoji
#         await message.channel.send(emoji)
#     elif message.content.startswith('$10emoji'):
#         emojis = [gen_emoji() for _ in range(10)]  # Generate a list of 10 random emojis
#         await message.channel.send(' '.join(emojis))
#     elif message.content.startswith('$heh'): # If the message starts with $heh, generate a string of "he" repeated a certain number of times
#         if len(message.content) > 4:
#             count_heh = int(message.content[4:])
#         else:
#             count_heh = 5
#         await message.channel.send("he" * count_heh)
#     else:
#         await message.channel.send("Command not recognized. Please use $halo, $bye, $pwgen, $cflip, or $emoji.")
#
# client.run("*** NO ***")
